services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: smitvi
      POSTGRES_PASSWORD: smitvi
      POSTGRES_DB: smitvi_media
    ports:
      - "5432:5432"
    volumes:
      - smitvi_pg:/var/lib/postgresql/data

volumes:
  smitvi_pg:
